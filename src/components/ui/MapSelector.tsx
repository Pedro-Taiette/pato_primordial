// This UI component simply re‑exports the main MapSelector implementation
// from the root of the components directory. Use this import when
// referencing the map inside UI containers. The actual Leaflet integration
// and map logic lives in `src/components/MapSelector.tsx`.

export { default } from "../MapSelector";